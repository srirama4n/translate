# Translation UI (React)

React frontend for the translation management system.

## Setup

```bash
cd ui
npm install
```

## Environment

Create `ui/.env`:

```
VITE_API_URL=http://localhost:8000
```

## Run

```bash
npm run dev
```

UI: http://localhost:5173

Ensure the API is running on port 8000.
